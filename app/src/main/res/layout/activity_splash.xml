<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/poster"
        tools:context=".SplashActivity">

        <LinearLayout
            android:id="@+id/overlayView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#DF04103A"
            android:gravity="center"
            android:orientation="vertical">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/splashIcon"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:src="@drawable/ic_m_movie_icon" />

            <androidx.appcompat.widget.AppCompatTextView
                style="@style/AppLargeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:text="@string/app_name" />


        </LinearLayout>

        <androidx.appcompat.widget.AppCompatTextView
            style="@style/AppSmallText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:text="@string/slogan_text"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@+id/overlayView"
            app:layout_constraintLeft_toLeftOf="@id/overlayView"
            app:layout_constraintRight_toRightOf="@id/overlayView" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>